<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ua.social.network.query.UserMapper">

    <sql id="coreUserAttributes">
      users.id,
      users.name,
      users.last_visit as lastVisit,
      file.file_path as avatar
    </sql>

    <select id="getUser" resultMap="UserRM">
        SELECT
        <include refid="coreUserAttributes"/>,
        f.id as friendId,
        f.name as friendName
        FROM users
        INNER JOIN file ON users.avatar_id = file.id
        LEFT JOIN user_friends ON users.id = user_friends.user_id
        LEFT JOIN users f ON users.id = user_friends.friend_id
        <where>
            id = #{userId}
        </where>
    </select>

    <resultMap id="UserRM" type="User">
        <id column="id"/>
        <association property="friends">
            <id column="friendId"/>
            <result column="friendName" property="name"/>
        </association>
    </resultMap>
</mapper>